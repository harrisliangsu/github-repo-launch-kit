# 最小 CI workflow 模板
# 放到 .github/workflows/ci.yml；按项目语言把 <BUILD>/<LINT>/<TEST> 替换为真实命令。
# 一个原则：CI badge 红 比 没有 badge 更糟，所以一开始只跑能稳过的检查。
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]

    steps:
      - uses: actions/checkout@v4

      # === Python 项目 ===
      # - uses: actions/setup-python@v5
      #   with:
      #     python-version: "3.10"
      # - run: python -m pip install -r requirements.txt
      # - run: python -m compileall <pkgs>
      # - run: python -m pytest -q

      # === Node 项目 ===
      # - uses: actions/setup-node@v4
      #   with:
      #     node-version: "20"
      # - run: npm ci
      # - run: npm run lint
      # - run: npm test

      # === Bash 项目 ===
      # - run: bash -n <script>
      # - run: ./test/test.sh

      # === 通用占位（先确保 CI 是绿的，再逐步加）===
      - name: Smoke
        run: echo "CI placeholder — replace with real checks"
